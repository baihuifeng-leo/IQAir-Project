# 电商工作台 · 设计系统文档

内部工具，服务小家电品类团队的日常数据工作：价格带沙盘、竞品对位、评论分析、竞品3D对比、个人销售报告、素材关键词合规检测。零框架——原生 JS/CSS，无构建步骤，单文件 `styles.css` 承载全部设计令牌与样式。本文档描述这套视觉语言的结构、原则和取舍，供后续新功能对齐参考。

## 1. 设计定位

**"暗色控制台（决策）+ 纸面（呈现）"** 是贯穿全站的底层比喻：顶栏和交互控件是深色、高信息密度的"控制台"层；6 个功能视图的主体内容区是"纸面"（`.paper` / `.rv-canvas`），随主题在深浅之间切换，但始终保持与控制台一致的冷色调，不做暖色纸张的拟物化处理——这是产品早期尝试过（米白纸面+金色晕染）又被拍板推翻的方向，现在统一收进"深空/瓷白"两套语言里。

两套主题服务不同场景，不是简单的反色：

| | 深色 · 极光玻璃 | 浅色 · 商务瓷白 |
|---|---|---|
| 心智 | 数据駕驶舱、专注分析 | 日常摸数据，低疲劳度 |
| 顶栏 | 半透明玻璃 + 极光光斑漂移动效 | 半透明白毛玻璃，无极光 |
| 阴影 | 纯黑投影 | 柔和蓝灰/彩色投影替代灰阴影 |
| 特例 | 竞品3D预览场景本体永远深空，不跟随主题（比稿定稿） | — |

主题偏好存 `localStorage`（`wb.theme`）先行渲染避免闪烁，登录后再用账号侧的偏好校正一次。

## 2. 设计令牌（`styles.css` `:root`）

### 2.1 色彩

**中性色阶**（深色态数值，浅色态见 `:root[data-theme="light"]` 覆写）：

| 令牌 | 深色 | 浅色 | 用途 |
|---|---|---|---|
| `--bg` | `#080c14` | `#eef1f7` | 页面底色 |
| `--surface` / `--surface-2` | `#101725` / `#0c1220` | `#ffffff` / `#f8f9fc` | 卡片/浮层底色 |
| `--text` | `#e9eef8` | `#1b2740` | 正文 |
| `--dim` | `#8da0bd` | `#46557a` | 次级文字 |
| `--dimmer` | `#5c6b87` | `#97a3bd` | 弱化文字（说明、时间戳） |
| `--line` / `--line-soft` | 边框，两级强弱 | | |

`--dim` / `--dimmer` 的具体数值是被真实反馈反复调过的——注释里记着"深色不够白""浅色不够黑"的历史：`--dimmer` 曾经只有约 2.6:1 对比度被反馈看不清，现在保持在 WCAG AA 门槛（4.5:1）附近。**改这两个值之前，先确认新数值过对比度检查，不要凭观感回退。**

**品牌 / 语义色**：

| 令牌 | 值（深色） | 含义 |
|---|---|---|
| `--mint` | `#4ee0c1` | 主品牌色：激活态、强调、成功态趋近色 |
| `--blue` | `#5b8cff` | 次强调、信息类 |
| `--aurora-1` / `--aurora-2` | `#8b5cf6` / `#4ee0c1` | 顶栏极光装饰专用 |
| `--ok` / `--warn` / `--bad` | `#35d07a` / `#f4a63b` / `#ef6b5e` | 三态语义色（阶段二引入），各自配 `-text` 变体给小号文字用——**不要直接拿主色当文字色**，对比度不够 |
| `--adv` / `--dis` | `#7ea6ff` / `#ff7b6e` | 优势/劣势对比（竞品对位表专用） |

浅色态不是深色态简单调亮，而是每个语义色重新挑过一版更深、饱和度更低的取值（例如 `--mint` 深色 `#4ee0c1` → 浅色 `#1f9e85`），保证浅底上文字对比度独立达标，两态不能用同一套数值反色实现。

**分类色**：矩阵/沙盘的品类标签允许用户用原生 `<input type="color">` 自定义颜色（`.tag-row`），不是固定调色板——这是唯一"用户可自定义色彩"的地方。

### 2.2 字体

```css
--f-display: "Bahnschrift", "DIN Alternate", "HarmonyOS Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
--f-body:    -apple-system, "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", "Segoe UI", sans-serif;
--f-mono:    ui-monospace, "JetBrains Mono", "SFMono-Regular", "Cascadia Mono", Consolas, monospace;
```

- **Display**（`--f-display`）：页面大标题（`.paper-head h1`、`.rv-head h1`），字重 700，字号 30px，常配负字距（`-0.2px`）营造紧凑的标题感；评论风向标标题额外叠加薄荷→蓝的渐变文字。
- **Body**（`--f-body`）：正文默认，全站基准字号 `15.5px`（移动端降到 `15px`）。
- **Mono**（`--f-mono`）：数据类数字（统计卡片 `.stat b`、`.p3d-stat b`）、代码/OCR 原文展示（`.mc-ocr-text`）——凡是"要精确对齐、像数据"的场合用等宽。

全站没有独立的排版比例尺（type scale）文件，字号按场景直接写死在对应组件规则里，一般落在 10.5px（大写字母间距标签）到 30px（页面大标题）之间。

### 2.3 动效

```css
--ease: cubic-bezier(0.22, 1, 0.36, 1);
--fast: 0.16s var(--ease);
--slow: 0.42s var(--ease);
```

统一的缓动曲线，`--fast` 用于 hover/焦点等即时反馈，`--slow` 用于视图切入、tab 下划线滑动等有位移感的过渡。`@media (prefers-reduced-motion: reduce)` 关闭极光漂移等纯装饰性动画。

## 3. 页面骨架

```
┌─────────────────────────────────────────────┐
│ .topbar (58px, 极光/毛玻璃)                    │
│  品牌标记 · 6 个 tab · 主题切换/编辑锁/在线成员/  │
│  保存状态/撤销重做/⋯更多菜单                    │
├─────────────────────────────────────────────┤
│ #workspace                                   │
│  .view（6 个视图绝对定位叠放，用 hidden 控制显隐） │
└─────────────────────────────────────────────┘
```

顶栏 tab 用一条 `.tab-ink` 下划线做位移动画（`left`/`width` 过渡，非重新渲染），六个视图分别是 `价格带沙盘 / 竞品对位 / 评论风向标 / 竞品3D预览 / 报告管理 / 素材质检`。视图切换不是路由跳转，是 `.view[hidden]` 的显隐 + `rise` 入场动画（透明度 + 上移 10px）。

**"编辑锁"是全局交互原则**：默认只读态（防止浏览时误改协同文档），`#btn-edit` 显式开启编辑才能改矩阵/对位表。只读态不是逐个给几十个 input 加 `disabled`，而是一个根 class（`.readonly` 之类）统一拦截交互——只读下仍需要能关弹层，所以有显式的例外规则。这个模式只覆盖矩阵/对位两个协同文档视图；评论/3D预览/报告/素材质检是表单/导入驱动，没有这层锁（素材质检有自己的词库编辑权限体系，见第 6 节）。

### 3.1 两种视图布局archetype

站内 6 个视图分两条谱系，不是每个都各自发明布局：

**A. 纸面画布（`.paper`）**——矩阵、对位表专用。有冷色晕染径向渐变背景，标题行直接内联可编辑（`contenteditable`），工具（导出等）就地放进标题行右侧，不设独立工具条也不设侧栏。这是"表格/网格"心智的视图。

**B. `.rv-canvas` 标题栏视图**——评论风向标、竞品3D预览、报告管理、素材质检共用这套骨架：深色发光背景（`radial-gradient` 薄荷+蓝双光斑 + 深色底），标题用渐变文字（`background-clip: text`），标题行右侧放工具条 `.rv-head-tools`（语言切换/导入按钮/分段控件等），主体内容在下方独立滚动区 `.rv-scroll`。这是"仪表盘/工作台"心智的视图，去掉了独立侧栏，操作全部收进标题行。

新增视图时先判断落在哪个谱系里——是"编辑一份共享文档"（走纸面画布 + 协同编辑锁）还是"浏览/操作一套数据"（走 `.rv-canvas` + 标题栏工具条），不要各起炉灶。

## 4. 组件库

### 4.1 按钮

| 类 | 场景 | 视觉 |
|---|---|---|
| `.ghost` | 次要操作，顶栏/工具条默认选项 | 透明底 + 描边，hover 加深 |
| `.solid` | 强调操作（保存、导入 Excel） | 薄荷→青绿对角渐变 + 发光投影 |
| `.editbtn` | 编辑锁开关 | 独立样式，见顶栏 |
| `.mc-btn` / `.mc-btn-primary` / `.mc-btn-danger` | 素材质检模块内 | 复用 `.ghost`/`.solid` 的尺寸语言但是模块自己的类名 |

`.solid` 有个记录在案的坑：它原本是给旧版侧栏画的整行按钮（`width:100%`），后来评论风向标/3D预览/报告管理的标题行直接复用这个类塞进横排工具条，没有重置宽度和内边距，导致同排按钮"更粗更高"。**任何时候把 `.solid`/`.ghost` 从纵向侧栏语境搬到横排工具条语境，要显式核对尺寸是否需要用 `.rv-head-import .solid` 这类限定选择器覆写**，别指望肉眼看着还行。

### 4.2 Tab / 分段控件

三层不同量级的 tab，视觉语言递减但同源（胶囊形状 + 激活态用 `--mint-dim` 背景/`--mint` 文字）：

1. 顶栏主导航 `.tabs .tab` — 下划线指示器，六个一级视图
2. 视图内子导航 `.mc-subtab`（检测台/历史记录/关键词库）、`.rpt-page-switch`（报告分页）— 分段控件，胶囊容器包裹
3. 卡片内二级 tab `.mc-utab`（素材质检关键词库的通用词分组）— 同款胶囊但不带外层容器背景，更轻量

新增分段控件时复用胶囊 + `--mint-dim` 激活态这套语言，不要另起配色。

### 4.3 卡片

`--group-card` 令牌 + `--hair` 描边 + 圆角是站内"卡片容器"的标准配方（竞品对位表的品牌分组 `.cmp-group`、素材质检关键词库的 `.mc-lib-card` 都用这套）。统计类小卡片（`.stat`/`.p3d-stat`）走更轻的 `--overlay-weak` 背景 + 细描边，字号更克制，用等宽字体突出数字。

### 4.4 Chip / 徽标

- `.chip` — 矩阵/对位表的标签芯片，可配色（跟随分类颜色）
- `.mc-chip` — 素材质检关键词芯片，`.mc-chip-bad`/`.mc-chip-warn` 表达检测结果的语义色
- `.mc-cat-badge` — 关键词分类彩色徽标（8 类各一色，深浅主题分别取色），2026-07-23 从纯文字 `[类别]` 标注改造而来，是"用颜色替代文字辨识"的最新案例
- `.log-act` — 变更日志动作徽标（编辑/登录/用户/备份/危险操作五色），深浅两套取色

这四套徽标是各自模块独立发展出来的，颜色含义不互通（`.mc-chip-bad` 的红色是"检测不合规"，不是"危险操作"），**新增徽标前先看是否已有同语义的徽标系统可以复用配色公式，而不是每个模块各发明一套**。

### 4.5 表单控件

统一配方：`padding: 7px 10px; border-radius: 8px; border: 1px solid var(--line-soft); background: var(--field-bg);`，聚焦态描边变 `--mint`。日期/颜色等原生控件通过 `color-scheme` 让浏览器自带的选择器 UI 也跟主题走。

### 4.6 浮层

- `.menu` — 下拉菜单，`pop` 弹出动画，分组用 `.menu-group` + 顶部细线分隔，危险操作单独一组并标红竖线
- `.sheet` / `.sheet-mask` — 模态弹层（用户管理、微盟数据填表等）
- Lightbox — 原生 `<dialog>` 实现看大图，白嫖浏览器自带的 Esc 关闭和焦点陷阱，不自己造轮子
- `.toaster` / `.toast` — 右下角提示条

### 4.7 状态反馈

- `.saveflag` — 顶栏持久化状态指示器，三态（`idle`/`saving`/`error`）用圆点颜色 + 呼吸动画表达
- `.mc-progress` — 素材质检上传进度条，`--mint` 填充色 + 宽度过渡
- `.mc-spin` — 小型加载环，纯 CSS `border-top-color` 旋转，没有依赖任何图标库

## 5. 各模块的差异化设计

虽然共享一套令牌，但各模块被有意做出可辨识的个性，不是同一模板换文字：

| 模块 | 个性 | 具体手法 |
|---|---|---|
| 价格带沙盘 / 竞品对位 | 电子表格质感 | `.paper` 冷白/深蓝纸面，`display: grid` 网格线用 `--hair`，标题栏内联可编辑，sticky 首行首列冻结 |
| 评论风向标 | "暗色、发光、数据感"（CSS 注释原话） | 双光斑径向渐变背景 + 渐变文字标题，热力图矩阵 |
| 竞品3D预览 | 深空场景，唯一不跟随主题的模块 | Three.js 场景色板独立管理（`--p3d-*` 令牌），CSS2D 文字带辉光 `text-shadow`，全屏态令牌显式钉死深色避免 `:fullscreen` 时机下 `var()` 重算滞后的渲染 bug |
| 报告管理 | 双身份：日常编辑 + "放映模式" | 放映模式是全屏、去除一切非内容元素、数字放大的演示态，服务周报汇报场景 |
| 素材质检 | 表单/卡片密集的管理台 | 关键词库页面（2026-07-23 刚重排）用卡片分组 + Tab 折叠通用词、彩色分类徽标，是站内"信息密度最高但要求快速辨识"的界面 |

新功能落地时先问：这个模块的"心智原型"更接近哪一个（表格/仪表盘/沉浸式可视化/演示/管理台），再决定视觉语言，而不是默认套用最近做的那个模块的样式。

## 6. 权限对视觉的影响

素材质检的关键词库有独立的三态权限（无权限/只读/编辑），只读态整卡片 `pointer-events: none` + 透明度降低 + 顶部警示文案（`.mc-warning`），**不是隐藏功能，而是可见但明确"看得见改不了"**——这和矩阵/对位表的全局编辑锁是两套独立的权限系统，分别服务"防误改的临时锁"和"分角色的常驻权限"两种不同需求，新增权限相关 UI 时先分清楚是哪一种。

## 7. 响应式

断点只有一个：`max-width: 760px`。核心策略不是把表格拆成纵向卡片流（会丢失横向对比能力），而是**保留横向滚动 + `scroll-snap-type: x proximity`**，让矩阵/对位表在手机上滑动时干净地停在一列上。触控目标统一不小于 30px，悬浮预览类交互（`.imgpeek`）在触屏上直接隐藏（触屏没有 hover）。

## 8. 打印 / 导出

`@media print` 只保留纸面内容，其余控制台层全部隐藏；矩阵导出为图片时（`matrix.js`）强制切回浅色配方，不依赖"常亮纸面"这种已经被否决的旧方案——深浅主题的选择权始终在用户，导出场景单独兜底，不影响主题系统本身的设计。

## 9. 给后续开发的检查清单

新增视图/组件前，对照这份清单：

- [ ] 颜色是否都来自 `:root` 令牌？深浅两态是否都手动覆写过（不是简单反色）？
- [ ] 新增语义色是否有 `-text` 变体给小号文字用？
- [ ] 这个视图属于"纸面画布"还是"`.rv-canvas` 标题栏"谱系？有没有强行拼出第三种骨架？
- [ ] 复用 `.ghost`/`.solid`/`.mc-btn` 时是否检查过尺寸语境（纵向侧栏 vs 横排工具条）？
- [ ] 新徽标/chip 颜色含义是否和已有系统（检测状态/变更日志/分类标签）冲突？
- [ ] 移动端断点下是否验证过横向滚动 + snap，而不是简单塞进单列？
- [ ] 深色态的装饰性动效（极光漂移等）是否遵守 `prefers-reduced-motion`？
